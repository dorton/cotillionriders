

<%= form_for([@user, @user.students.build]) do |f| %>
  <div class="mdl-textfield mdl-js-textfield mdl-textfield--floating-label">
    <%= f.label :first_name, class: "mdl-textfield__label" %>
    <%= f.text_field :first_name, class: "mdl-textfield__input" %>
  </div>
  <div class="actions">
    <%= f.submit "Update user", class: "mdl-button mdl-js-button mdl-button--raised" %>
  </div>
<% end %>
